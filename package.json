{
  "name": "quality-dashboard-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Quality Dashboard - test/tooling package (Lambda runtime deps live in src/package.json)",
  "type": "commonjs",
  "scripts": {
    "test": "jest --colors",
    "test:ci": "jest --ci --colors",
    "test:frontend": "npm --prefix frontend run test",
    "build:frontend": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "js-yaml": "^4.1.0",
    "aws-sdk-client-mock": "^4.0.1",
    "mysql2": "^3.11.0",
    "ssh2-sftp-client": "^11.0.0",
    "@aws-sdk/client-s3": "^3.650.0",
    "@aws-sdk/client-secrets-manager": "^3.650.0",
    "@aws-sdk/client-sqs": "^3.650.0",
    "@aws-sdk/client-sns": "^3.650.0",
    "@aws-sdk/client-ssm": "^3.650.0"
  }
}
